spring:
  application:
    name: boardAWS
---
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fisa
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true